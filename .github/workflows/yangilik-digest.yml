name: Yangilik digest scheduler

on:
  schedule:
    - cron: "0 5 * * *"   # har kuni 10:00 (Toshkent)
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: yangilik-digest-production
  cancel-in-progress: false

jobs:
  run-yangilik-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Call production yangilik digest endpoint
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          MEDICAL_NEWS_CRON_URL: ${{ secrets.MEDICAL_NEWS_CRON_URL }}
        shell: bash
        run: |
          missing=()
          [[ -z "$CRON_SECRET" ]] && missing+=("CRON_SECRET")
          [[ -z "$MEDICAL_NEWS_CRON_URL" ]] && missing+=("MEDICAL_NEWS_CRON_URL")
          if (( ${#missing[@]} > 0 )); then
            echo "Missing GitHub Actions secrets: ${missing[*]}" >&2
            echo "Configure them in Repository settings > Secrets and variables > Actions." >&2
            exit 1
          fi

          # Ingestion URL'idan digest endpoint'ini hosil qilamiz (yangi secret shart emas).
          DIGEST_URL="${MEDICAL_NEWS_CRON_URL%/*}/yangilik-digest"

          curl --silent --show-error --fail-with-body \
            --connect-timeout 15 \
            --max-time 290 \
            --request GET \
            --header "Authorization: Bearer ${CRON_SECRET}" \
            "${DIGEST_URL}"
